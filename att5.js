// Função para calcular área
function calculaArea(largura, altura) {
    return largura * altura;
}

console.log("Área:", calculaArea(5, 10)); // Exibe: Área: 50

// Função de saudações
function saudacao(nome) {
    console.log(`Olá, ${nome}!`);
}

saudacao("Seu Nome"); // Substitua "Seu Nome" pelo seu nome real
